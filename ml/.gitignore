# Trained weights - large, regenerated by training, pulled from Modal.
models/*.pt

# Raw footage and extracted frames - far too big for git. Back these up
# somewhere else (a NAS, a shared drive); they are the actual dataset and
# the one thing here that cannot be regenerated.
recordings/
datasets/raw/
*.mp4
*.avi

# Per-machine state
calibration.json
pending_goals.json
venv/
.verify-venv/
__pycache__/
*.pyc
